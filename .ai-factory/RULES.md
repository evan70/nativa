# Project Rules

## Packages Directory
- **NEVER** modify files inside `packages/` directory. The `packages/` directory contains Marko framework packages — changes there are not allowed.
- Fix framework integration issues in the consuming module or application code instead.
