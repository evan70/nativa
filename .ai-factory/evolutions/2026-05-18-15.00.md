# Evolution: 2026-05-18 15:00

## Intelligence Summary
- Patches analyzed: 1 (2026-05-15-11.55.md — EntityCollection + template resolver)
- Recurring patterns: PHPUnit mock conflicts, module-specific DB migrations not auto-discovered, `db:migrate` command not available
- Tech stack: PHP 8.5+, Marko Framework, SQLite, PHPUnit (not Pest), PHPStan level max

## Improvements Applied

### aif-fix
- Added PHPUnit mock conflict workaround: avoid mocking classes with `method()` method (like `Request`) — use real objects instead — patch-driven
- Added Response status check pattern: use `getStatusCode()` or reflection if statusCode is private — patch-driven

### aif-implement
- Added module-specific DB migration note: `modules/{name}/database/migrations/` files are NOT auto-discovered by `php marko db:migrate` — use manual SQL execution or main `database/migrations/` directory — patch-driven
- Added migration fallback pattern: use `php -r` with PDO direct SQL execution when migration system doesn't support module-specific DBs — patch-driven

### aif-review
- Added checklist item: verify migration file is in auto-discovered directory or document manual execution method — patch-driven
- Added checklist item: verify PHPUnit mock doesn't conflict with class methods named `method()` — patch-driven
- Added checklist item: check Response property visibility before accessing in tests — patch-driven

### aif-plan
- Added task guidance: for module-specific DB migrations, check if `db:migrate` discovers the migration path first — patch-driven
- Added testing subtask: when writing controller tests, prefer real Request objects over mocks to avoid `method()` conflict — patch-driven

### marko-framework
- Added `ViewInterface::include()` method convention for partial template rendering — conventions
- Added session-driver note: project uses `marko/session-file` not `marko/session-database` — tech-stack
- Added vendor discovery pattern: `ModuleDiscovery::discoverInVendor()` now handles flat packages/ structure natively — no more vendor/marko symlinks needed — tech-stack

## Patterns Identified
- `#phpunit-mock-method`: Request class has a `method()` method that breaks PHPUnit's `createMock()` — use real objects or `partialMock()` with excluded methods
- `#migration-discovery`: db:migrate only scans `database/migrations/` — module-specific DBs need manual SQL execution or a different approach
- `#session-driver`: project uses file-based sessions now (session-file), not database
- `#simpleview-include`: `ViewInterface` now has `include()` for partial templates — important for template composition
- `#vendor-flat-discovery`: ModuleDiscovery handles flat packages/ — no more `createVendorSymlinks()` in build.php
- `#controller-registration-pattern`: RegisterController follows LoginController pattern (attribute routing, constructor DI, guard check, ViewInterface::render for Response)
