# Evolution: 2026-05-15 11:23

## Intelligence Summary
- Patches analyzed: 0 (first evolution run)
- Recurring patterns: N/A (no patches yet)
- Tech stack: PHP 8.5+, Marko Framework, SQLite, Pest, PHPStan level max

## Improvements Applied

### marko-framework
- Extended module.php with full field reference (preferences, boot, plugins) ← conventions
- Added extended module metadata: group, routes, idleTimeout, isCore, middleware ← conventions
- Added vendor symlink pattern for flat vendor discovery ← conventions
- Added ModuleGroupManager & idle eviction section ← conventions

### aif-fix
- Replaced error_log() with LoggerInterface pattern for Marko Framework ← conventions
- Added Marko-specific checks section: binding conflicts, nullable Container resolve, vendor symlink, DependencyResolver ← tech-stack

### aif-implement
- Added module creation checklist (composer.json, module.php, PSR-4, DB mapping) ← conventions
- Extended PHPStan verification with custom phpstan.neon and Marko-specific type traps ← conventions

### aif-review
- Added 7 Marko-specific checklist items: binding conflicts, module metadata, symlink, DB mapping, eviction config ← tech-stack

### aif-plan
- Added module creation checklist item to requirement analysis ← conventions

## Patterns Identified
- Module binding conflicts: requiring preference registry or empty module.php for conflicts
- Container resolve() nullable param gap: upstream bug, workaround via bindings or callback removal
- vendor/marko symlink: critical for ModuleDiscovery, packages cannot live in packages/ without it
- ModuleGroupManager metadata: group/routes/idleTimeout/isCore not documented in base skill
- Database module mapping: config/database.php['modules'] array as a project-specific convention
