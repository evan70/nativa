# Evolution: 2026-05-07 12:00

## Intelligence Summary
- Patches analyzed: 0 (no new patches since last evolution)
- Recurring patterns: N/A (insufficient patch data)
- Tech stack: PHP 8.5+, Marko Framework, SQLite, pnpm

## Improvements Applied

### /aif-review
- Added PHP/Marko Framework Specific checklist section:
  - `declare(strict_types=1)` requirement
  - No `new` in constructors — use DI
  - `readonly` classes
  - PHPStan level 6
  - Routing attributes (`#[Get]`, `#[Post]`, etc.)
  - Entity attributes (`#[Table]`, `#[Column]`)
  - Module bindings in `module.php`
  - File placement conventions (Controllers, Entities)

### /aif-fix
- Enhanced PHP/Marko Framework Context section with common error patterns:
  - DI resolution failures → check module.php bindings
  - Migration errors → `php marko db:migrate`
  - Module not found → verify `extra.marko.module: true`
  - Routing errors → check controller attributes
  - Entity mapping errors → verify Table/Column attributes
- Added logging pattern: `log_add_debug($message, $context)`
- Added environment note: `LOG_LEVEL=debug` in .env

### /aif-implement
- Added PHP/Marko Framework Implementation Notes section:
  - File placement conventions for all Marko file types
  - `declare(strict_types=1)` requirement
  - Controller pattern with readonly + attributes + DI
  - Entity pattern with Table/Column attributes
  - Module system pattern
  - View rendering with assets pattern
  - Logging pattern

### /aif-best-practices
- Added PHP/Marko Framework Conventions section with:
  - Class design example (readonly controller with DI and attributes)
  - Entity design example (Table/Column attributes)
  - Module definition example (bindings/singletons)
  - 7 Marko-specific rules
- Updated Quick Rules Summary table with PHP/Marko row

### /aif-plan
- Added PHP/Marko Framework Task Guidance section:
  - File path conventions for all Marko file types
  - `declare(strict_types=1)` requirement
  - Controller/Entity/Module task specifics
  - CLI command reference (`php marko`)
  - Testing directory convention

## Patterns Identified
- Second evolution run — still no patches to analyze
- Project conventions identified from codebase:
  - `readonly` classes with constructor injection (HomeController)
  - Routing attributes (`#[Get('/')]`)
  - Entity attributes (`#[Table]`, `#[Column]`)
  - Module system with `module.php`
  - View adapter pattern (ViewAdapter wrapping ViewInterface)
  - Page layout detection (PageLayout)
  - PSR-4 autoloading under `app/src/`
  - Configuration via `config/` and `.env`
  - SQLite database in `storage/data/`

## Next Steps
1. Run `/aif-review` on future code to verify improvements
2. After 5-10 bug fixes, run `/aif-evolve` again to analyze patch patterns
3. Consider evolving `/aif-commit` with PHP/Marko-specific scope examples
