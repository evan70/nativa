# Evolution: 2026-05-15 11:58

## Intelligence Summary
- Patches analyzed: 1 (EntityCollection type mismatch + template resolution)
- Recurring patterns: upstream API changes after sync, container instance registration, template path fallback
- Tech stack: PHP 8.5+, Marko Framework, EntityCollection, ModuleTemplateResolver

## Improvements Applied

### aif-fix
- Added item 5: Upstream API changes after sync (EntityCollection → array_* breakage) — patch-driven
- Added item 6: TemplateNotFound — shared templates/ directory fallback — patch-driven
- Added item 7: Container instance registration for classes used in binding closures — patch-driven

### aif-implement
- Added `resources/views/` directory checklist item for modules with templates — patch-driven
- Added container instance registration verification for closure bindings — patch-driven

### aif-review
- Added 4 new checklist items: container instances, EntityCollection/array_* pattern, template directory, bootstrap/app.php registration — patch-driven

## Patterns Identified
- `#entity-collection` `#array-type`: upstream Repository API returns EntityCollection, not array — callers using array_filter/array_map/array_values break
- `#template-resolution`: ModuleTemplateResolver only searches module's resources/views/ — shared templates/ dir needs custom fallback resolver
- `#container-registration`: Application and other key classes need explicit `$container->instance()` in bootstrap — not autowired
