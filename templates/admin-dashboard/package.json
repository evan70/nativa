{
  "name": "@nativa/admin-dashboard",
  "version": "0.0.1",
  "description": "Vanilla JS and CSS Admin Dashboard for Nativa CMS",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "rm -rf ../../public/mark && vite build && touch ../../public/mark/.nojekyll",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
