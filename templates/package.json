{
  "name": "@nativa/vanilla-cards",
  "version": "0.0.1",
  "description": "Vanilla JS and CSS cards architecture for Nativa CMS",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "rm -rf ../public/dist && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@nativa/cardboard": "workspace:*",
    "@nativa/theme-fire-show": "workspace:*",
    "chart.js": "^4.5.1",
    "htmx.org": "^2.0.10"
  }
}
