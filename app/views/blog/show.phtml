<div class="stack">
    <div class="eyebrow">Article</div>
    <h1><?= htmlspecialchars($article->title, ENT_QUOTES, 'UTF-8') ?></h1>
    <p class="meta">
        <?= htmlspecialchars($article->createdAt ?? 'Draft', ENT_QUOTES, 'UTF-8') ?>
    </p>
    <div class="card">
        <div class="article-content"><?= htmlspecialchars($article->content, ENT_QUOTES, 'UTF-8') ?></div>
    </div>
    <div class="actions">
        <a class="button secondary" href="/blog">Back to blog</a>
    </div>
</div>
